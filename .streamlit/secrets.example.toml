# 이 파일을 복사해 .streamlit/secrets.toml로 저장하고 실제 값을 채워 넣으세요.
# .streamlit/secrets.toml은 .gitignore에 포함되어 깃에 올라가지 않습니다.
# smtp_password는 Gmail 계정 비밀번호가 아니라, 구글 계정 설정 > 보안 > 2단계 인증 >
# 앱 비밀번호에서 발급받은 16자리 "앱 비밀번호"를 사용해야 합니다.

[email]
smtp_user = "cchasuhyun@gmail.com"
smtp_password = "REPLACE_WITH_GMAIL_APP_PASSWORD"
